// 임시 파일 - 나중에 다른 분이 로직을 채울 예정
export const checkSubwayRealtime = async (noti) => {
    console.log(`[임시] ${noti.station_id}번 역 실시간 위치 조회 중...`);
    // 일단 항상 true를 반환하게 해서 발송 로직이 타는지 확인
    return true; 
};